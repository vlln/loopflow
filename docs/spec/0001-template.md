---
title: Spec Template 001
description: 一句话说明本设计文档覆盖的产品或功能模块。
type: spec
# status: 见 SKILL.md status 有效值表
status: proposed
version: 1
created: YYYY-MM-DDTHH:MM:SSZ
---

<!-- [项目名称] 规格文档。业务需求、数据模型均在此定义。验收标准（AC）在 [ac/](../ac/)，接口定义在 [interface/](../interface/)。

本文件是单文件示意，展示完整的 Spec 结构。实际编写时，应拆分为多篇文档：
- 概要设计部分保留在主文档
- 详细设计部分按模块独立为文件，主文档用引用链接

拆分指南见 [spec/README.md](README.md)。

Spec / Interface / ADR / AC 边界：
以下内容属于 Spec（WHAT，描述系统做什么）：
- 用户故事、模块划分、业务流程、数据交互需求
以下内容属于 Interface（WHAT，传输什么内容）：
- 接口的业务字段定义（字段名、类型、含义 —— 来自业务需求，非技术选型）
- 错误码（来自 AC 的异常/失败场景）
以下内容属于 ADR（MACRO，统一技术规则和标准）：
- 通信协议、数据序列化格式
- 统一错误码规范、分页标准、鉴权方式
- 存储选型、架构模式
- ADR 不定义某个业务接口的字段
以下内容属于 AC（HOW TO VERIFY，怎样算做完）：
- 验收标准（正常/边界/异常/失败四场景）
- AC 独立于 Spec，在 [ac/](../ac/) 中定义
以下内容属于 CONTRIBUTING.md（HOW，编码规范）：
- 语言版本（如 TypeScript 严格模式）
- 具体 npm 包名和版本号

写作原则：
- 不重复上层文档（Vision 已定义的内容不在此重述，引用即可）
- 接口定义优先使用可生成代码的契约格式（如 OpenAPI、TypeScript 接口声明）
- 模块提供的能力 = 可验证的功能列表（不是"负责用户相关功能"，而是"提供用户注册、登录、信息查询接口"）-->

---

# 概要设计

## 一、项目概述

<!-- 引用 [Vision](../vision.md)，此处仅补充本 Spec 特有的范围或边界。不重复 Vision 的业务目标和用户范围。 -->

---

## 二、用户故事

<!-- 从用户视角描述核心场景 -->

| 编号 | 角色 | 需求 | 目的 | 优先级 |
|------|------|------|------|--------|
| US-001 | | | | P0 |

---

## 三、模块划分

| 模块 | 提供的能力 | 目录路径 | 优先级 |
|------|-----------|---------|---------|
| | | | P0 |

<!-- "提供的能力"是模块对外的可验证功能列表（如"注册、登录、Token 刷新"），直接映射到 AC 和接口定义。不写"负责用户功能"这类模糊描述。 -->

---

# 详细设计

## 四、数据模型

### 表名

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | UUID | PK | 主键 |
| created_at | TIMESTAMP | NOT NULL | 创建时间 |

---

## 五、业务规则

<!-- 跨模块、跨接口的业务约束 -->

| 规则编号 | 描述 | 触发条件 | 约束 |
|----------|------|----------|------|
| BR-001 | | | |

---

## 六、UI 约束

### 页面结构

<!-- 页面路由、组件树、布局 -->

### 组件规范

<!-- 组件命名、Props 约定、状态管理。不写具体框架名和包名（属于 ADR/CONTRIBUTING）。 -->

### 视觉回归

<!-- 若有 Visual Regression Test 要求，在此定义基准和阈值 -->

---

# 约束

## 七、非功能指标

<!-- 量化指标，可用于专项测试验收。无指标时省略。 -->

| 维度 | 指标 | 目标值 |
|------|------|--------|
| 性能 | 响应时间 | — |
| 性能 | 并发数 | — |
| 安全 | 认证方式 | — |
| 可用性 | 系统 uptime | — |
| 兼容性 | 支持范围 | — |

---

## 八、依赖项

<!-- 本 Spec 依赖的外部能力（不写具体包名，写需要什么功能） -->

| 依赖 | 版本 | 用途 |
|------|------|------|
| | | |

---

## 九、术语表

| 术语 | 定义 |
|------|------|
| | |
