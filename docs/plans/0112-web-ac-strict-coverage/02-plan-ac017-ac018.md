---
title: AC-017 Loops 工作区 + AC-018 Backends 覆盖补齐
description: 为 AC-017 的 8 个和 AC-018 的 8 个 planned 场景编写真实测试节点，必要时补产品行为
type: plan
status: pending
created: 2026-07-30T00:00:00Z
---

# Context

AC-017（Loops 工作区与文件预览）planned：N-1（主从选择）、N-2（文本/图片/PDF 预览与 raw Content-Type）、N-3（Run file changes 二进制预览）、B-1（空 agents 目录）、B-2（非白名单/超限 422）、F-1（Loop 删除后 404 与左栏刷新）、F-2（非法 YAML 标记 invalid）、F-3（raw 读取 OSError → 500 file_read_failed 不发 200/部分 bytes）。AC-018（Backends）planned 全部 8 个：N-1（列表真实诊断字段）、N-2（stderr token 脱敏）、B-1（空状态）、B-2（version null → Unknown）、E-1（诊断超时 100ms）、E-2（非法编码替换字符）、F-1（404）、F-2（启动失败 503 不伪造指标）。E-1/E-2 已在 0111 保留（安全路径），此处不涉及。分支 `feat/0112-ac017-ac018`。

# Request

1. 为 16 个场景各写真实测试节点：后端 API 语义用 `tests/integration/test_web_api.py` / `tests/unit/test_web_resources.py`，UI 语义（左栏刷新、空状态、Unknown 显示、脱敏 DOM）用 `web/src/App.test.tsx`。
2. TDD：先红后绿；产品行为不符冻结契约时补最小实现。
3. `TEST_NODES` 仅追加 AC-017/AC-018 段，重新生成 `cases.json`。
4. 验证 strict 下这两段 0 planned，其余段计数不变（49）。

# Constraints

- 不改契约文档；契约存疑停下来上报。
- AC-017-F-3 必须断言"未先发送 200 headers 或部分 bytes"，不得弱化为仅检查状态码。
- AC-018-N-2 必须同时断言 API 与 DOM 不含明文 token。
- AC-018-E-1 超时阈值固定 100ms fixture，不得放宽。
- 只追加 `TEST_NODES` 本段条目；文档与代码分开提交；commit 标注 AC 编号。

# Checkpoint

- [ ] 16 个测试节点有实质断言（HTTP 语义 + DOM 语义各自落地）
- [ ] MR 门禁通过
- [ ] strict 检查 AC-017/AC-018 段清零，其余段计数不变
- [ ] Report 留档，可反向定位
